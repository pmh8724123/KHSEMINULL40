<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">

	<select id="loginMember" resultType="Member">
		SELECT * FROM member WHERE mem_id = #{memId} AND mem_pw = #{memPw} AND status = 'Y'
	</select>
	
	<insert id="insertMember">
		<selectKey keyProperty="memNo" resultType="int" order="BEFORE">
			SELECT SEQ_MNO.NEXTVAL FROM DUAL
		</selectKey>
		
		INSERT INTO member VALUES(SEQ_MNO.CURRVAL, #{memId}, #{memPw}, #{memName}, #{deptNo}, #{phone}, #{email}, DEFAULT, DEFAULT)
	</insert>
	
	<select id="selectUniList" resultType="University">
		SELECT uni_no, uni_name FROM university WHERE uni_no != 0
	</select>
	
	<select id="selectDeptList" resultType="Department">
		SELECT dept_no, dept_name FROM department WHERE uni_no = #{uniNo}
	</select>
	
	<select id="loadUserByUsername" resultType="Member">
		SELECT * FROM member WHERE mem_id = #{userName}
	</select>
	
	<select id="updateMemberStatus" resultType="Member">
		UPDATE MEMBER
		SET STATUS = #{status}
		WHERE MEM_NO = #{memNo}
	</select>
	
	
	
	
	
	
	
	
	
	
	
</mapper>