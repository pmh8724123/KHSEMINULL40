<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-5.7.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- 암오화 인코딩 기능을 가진 bean 객체 등록 -->
	<bean id="bcryptPasswordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>
	
	<security:http security="none" pattern="/resources/**"/>
	
	<security:http auto-config="true">
		
		<!-- 회원가입, 아이디 찾기 설정 -->
		<security:intercept-url pattern="/" access="permitAll"/>
		<security:intercept-url pattern="/member/**" access="permitAll"/>
		<security:intercept-url pattern="/member/register" access="permitAll"/>		
		<security:intercept-url pattern="/member/findIdPw" access="permitAll"/>
		
		<!-- 공지사항 접근 권한 설정 -->
		<security:intercept-url pattern="/notice/list" access="hasRole('USER')"/>
		<security:intercept-url pattern="/notice/detail" access="hasRole('USER')"/>
		<security:intercept-url pattern="/notice/**" access="hasRole('ADMIN')"/>
		
		<!-- 관리자 페이지 접근 권한 설정 -->
		<security:intercept-url pattern="/admin/**" access="hasRole('ADMIN')"/>
		<security:intercept-url pattern="/admin/university" access="hasRole('MASTER')"/>
		<security:intercept-url pattern="/admin/university/**" access="hasRole('MASTER')"/>
		
		<security:intercept-url pattern="/board/**" access="hasRole('USER')"/>
		<security:intercept-url pattern="/mypage/**" access="hasRole('USER')"/>
		<security:intercept-url pattern="/attendance/**" access="hasRole('USER')"/>
	
		<security:intercept-url pattern="/security/accessDenied" access="permitAll"/>
		
		
		
		<security:form-login login-page="/" login-processing-url="/member/loginProcess" 
			username-parameter="memId" password-parameter="memPw" 
			default-target-url="/board/list" authentication-failure-url="/member/login"/>
		<security:logout logout-url="/member/logout" invalidate-session="true" logout-success-url="/"/>
		
		<security:remember-me data-source-ref="datasource" token-validity-seconds="1209600"/>
		<security:access-denied-handler error-page="/member/waiting"/>
	</security:http>
	
	<context:component-scan base-package="com.kh.cam.member">	
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
	</context:component-scan>
	
	<!-- <context:component-scan base-package="com.kh.cam"/> -->
	

	<security:authentication-manager>
    	<security:authentication-provider user-service-ref="memberServiceImpl">
     		<security:password-encoder ref="bcryptPasswordEncoder"/>
   		</security:authentication-provider>
	</security:authentication-manager>
</beans>
